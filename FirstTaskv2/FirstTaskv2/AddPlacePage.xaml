<Page
    x:Class="FirstTaskv2.AddPlacePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FirstTaskv2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
	
    <StackPanel Height="640" VerticalAlignment="Top" Width="399.999969482422">
        <TextBox Header="Name" Text="{Binding Name, Mode=TwoWay}" Margin="0,10,0,0" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
        <TextBox Header="Address" Text="{Binding Address, Mode=TwoWay}" Margin="0,10,0,0" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
        <TextBox Header="Latitude" Text="{Binding Latitude, Mode=TwoWay}" Margin="0,10,0,0" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
        <TextBox Header="Longitude" Text="{Binding Longitude, Mode=TwoWay}" Margin="0,10,0,0" TextWrapping="Wrap" VerticalAlignment="Stretch"/>
        <CheckBox IsChecked="{Binding HasWifi, Mode=TwoWay}" Content="Has Wifi" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,20,0,0"/>
	</StackPanel>
 
</Page>
